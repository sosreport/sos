language: python
python:
  - 2.7
env:
  - DEPS=false
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-selinux rng-tools
install:
  - make gpgkey
  - sudo make install
script:
  - make test
