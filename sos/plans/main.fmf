# Run on localhost by default
provision:
    how: local

discover:
    how: fmf
    url: https://github.com/sosreport/sos-tests.git
    # ref: main  # Please specify the ref in separate test plan
    # filter: "tier: 0, 1, 2"  # Please specify the filter in separate test plan

# Use the internal executor
execute:
    how: tmt